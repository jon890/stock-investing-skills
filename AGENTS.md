# stock-investing-skills

주식 투자 의사결정 워크플로를 스킬로 만드는 저장소다.
핵심 가치와 워크플로 정의는 `docs/prd.md` 가 소유한다. 이 파일은 작업 규칙만 담는다.

## 구조

| 경로 | 담는 것 |
| --- | --- |
| `docs/prd.md` | 워크플로 정의와 핵심 가치 |
| `skills/<name>/SKILL.md` | 스킬 본문 |
| `scripts/` | 밸류에이션 계산 스크립트 |
| `reports/` | 날짜별 리포트 HTML 과 원자료 JSON |

## 데이터

valley.town 의 내부 API 를 쓴다. 로그인 세션이 필요하므로 브라우저 안에서 호출한다.
브라우저 조작은 `~/.claude/scripts/browser-driver` 를 쓴다. 직접 백엔드를 부르지 않는다.

| 용도 | 경로 |
| --- | --- |
| 섹터 히트맵 | `/api/financials/market-overview/sector-treemap?period=1d&contentType=USA\|KOR` |
| 종목 요약과 배수 | `/api/analysis/quote/summary?stockId=<TICKER>.<OQ\|N>&assetClass=STOCK` |
| 종목 페이지 | `/financials/quote/<TICKER>:US/summary` |

### 실측으로 확인한 것

- 히트맵은 canvas 로 그린다. 화면 글자를 읽을 수 없으므로 API 를 직접 부른다.
- `contentType` 은 `USA` 와 `KOR` 만 값을 준다. `KR` 은 200 을 주면서 빈 배열이다.
- 섹터 최상위 노드에는 이름이 없다. `trbcCode` 로만 구분한다. 대응은 아래 표를 쓴다.
- `summary` 는 `figi` 없이 `stockId` 만으로도 동작한다. 비교군을 한 번에 훑을 때 쓴다.
- `valuation` 배열의 `label` 은 회계연도가 아니라 역년이다.
  1월 종료 회계연도를 쓰는 기업은 한 해 밀린다.
- 조회 결과가 비면 로그인이 끊긴 것이다. `url` 명령으로 현재 주소를 먼저 본다.
- `stockId` 접미사는 거래소가 정한다. `NASD` 는 `.OQ`, `NYSE` 는 `.N`, `KRX` 는 `.KS` 다.
  틀린 접미사도 200 을 주면서 값만 비운다. 조용한 실패이므로 결측 개수를 세어 확인한다.
- 히트맵 유니버스는 시장당 약 490개다. 대형주에 치우쳐 소형 텐베거 후보가 적게 잡힌다.
- `period` 는 `1d 1w 1m 3m 6m ytd 1y 3y` 를 받는다.
- 4자리 그룹은 생각보다 넓다. `5710` 에는 반도체와 함께 애플이 들어 있다.
  그룹 단위 성장률이 대형 저성장주에 희석되므로 종목 단위로 한 번 더 본다.

### 회계연도가 역년과 어긋나면 환산한다

배수의 `label` 은 역년이다. 4월이나 1월에 회계연도를 닫는 기업은 그대로 맞지 않는다.
월수로 가중해 옮긴다.

```
역년 2027 = FY2027 × (겹치는 달 ÷ 12) + FY2028 × (나머지 ÷ 12)
```

CRDO 는 4월 종료라 역년 2027 이 FY2027 의 4개월과 FY2028 의 8개월로 이루어진다.

### 비교군 배수를 교체할 때는 두 해를 함께 바꾼다

한 해만 교체하면 성장률이 두 배수의 비율로 계산되어 무너진다.
MRVL 의 역년 2027 만 바꿨더니 성장률이 140.8% 로 잡혀 회귀의 적합 구간을 망가뜨렸다 (실측).

### 한국 시장의 한계

| 무엇 | 실측값 | 결과 |
| --- | --- | --- |
| 컨센서스 추정 보유율 | 493개 중 130개 (26%) | 수요와 가격결정력 팩터를 신뢰할 수 없다 |
| 시가총액 통화 | 원화 | 달러 기준 시총 상한을 그대로 못 쓴다 |

미국은 491개 중 402개 (82%) 다. 종목 선별은 미국에서 한다.

### TRBC 섹터 코드

| 코드 | 섹터 | 코드 | 섹터 |
| --- | --- | --- | --- |
| 50 | 에너지 | 55 | 금융 |
| 51 | 소재 | 56 | 헬스케어 |
| 52 | 산업재 | 57 | 기술 |
| 53 | 경기소비재 | 59 | 유틸리티 |
| 54 | 필수소비재 | 60 | 리츠 |

## 스크립트

| 파일 | 하는 일 |
| --- | --- |
| `scripts/fetch_universe.sh` | 시장 전 종목의 일곱 기간 등락과 밸류에이션 배수를 모은다 |
| `scripts/bottleneck.py` | 산업 그룹별 병목 점수를 낸다. 텐베거 후보도 여기서 나온다 |
| `scripts/valuation.py` | DCF, 역DCF, 상대가치, 손익비, 텐베거 판정을 한 번에 낸다 |
| `scripts/inputs/<티커>.json` | 종목별 가정. 계산 코드는 종목을 모른다 |

새 종목을 보려면 `inputs/` 에 파일을 하나 더 만든다. 계산 코드는 고치지 않는다.

## 계산

밸류에이션은 산문으로 설명하지 않고 `scripts/` 의 파이썬으로 계산한다.
리포트에 적은 숫자는 모두 스크립트 출력에서 나와야 한다.

- 이분법으로 근을 찾을 때는 대상 함수의 증감 방향을 양 끝에서 판정한다.
  방향을 가정하면 구간 끝값이 답으로 나와도 알아채지 못한다 (실측).
- 회귀식은 적합 구간 안에서만 쓴다. 밖이면 그 방법을 버리고 사유를 리포트에 적는다.
- SBC 는 주주의 실질 비용이므로 FCF 에서 차감한다.
- 10년 뒤 수익은 시나리오마다 최종 연도의 기업가치를 직접 구해 계산한다.
  `매출배수 × 마진배수` 로만 보면 배수가 10년 내내 그대로라고 가정하게 된다.
  매출이 10배가 된 기업은 성숙 기업 배수로 내려오므로 이 축소가 대부분을 상쇄한다.
  실측으로 CRDO 는 매출 10.8배에 배수 0.19배가 곱해져 2.1배가 됐다.
  이 축소를 빼면 텐베거 판정이 목표주가와 정반대로 나온다.
- 시장 규모는 최종 예측 연도와 같은 시점의 값을 쓴다.
  오늘의 시장 규모를 10년 뒤 매출과 비교하면 어떤 성장 기업도 항상 탈락한다 (실측).
  기준 연도가 다르면 코드가 그 판정을 건너뛴다.
- JSON 으로 왕복하는 값의 사전 키는 문자열로 만든다. 실수 키는 왕복 뒤 문자열이 되어 깨진다.

## 문체

리포트와 문서는 평서체로 쓴다. 한 산출물 안에서 문체를 섞지 않는다.
전역 규칙인 `~/.claude/rules/korean-style.md` 와 `writing-structure.md` 를 따른다.

## 산출물

리포트는 로컬 HTML 파일로 만든다. 외부에 게시하지 않는다.
파일명은 `reports/<종류>-<종목이나 시장>-<YYYYMMDD>.html` 로 한다.

## zsh 함정

`noclobber` 가 켜져 있어 `cat > 기존파일` 이 실패한다.
덮어쓰기 전에 `rm -f` 를 먼저 한다. 실패해도 옛 파일이 그대로 남아 조용히 옛 결과가 나온다 (실측).
